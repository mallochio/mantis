# 2026-08-01 Real-3B Conductor acceptance smoke (GCP L4 spot)

**Status:** succeeded — `valid_for_runtime=true`.

## What was validated
- `di-zhang-fdu/openfugu-conductor-3b` loaded in `bfloat16` on a single NVIDIA L4.
- One GRPO step (`steps=1`, `limit=1`, `num_generations=2`, `per_device_batch=2`) completed.
- `adamw_bnb_8bit` optimizer kept VRAM usage within 24 GB.
- Checkpoint saved, reloaded, and a single inference generation produced a parseable
  Conductor DAG (`format_reward=1.0`).

## Instance and cost
- Cloud: GCP, region `us-central1-c`
- Instance: `g2-standard-8` spot with 1x NVIDIA L4
- Wall-clock (provisioning + setup + train + acceptance): ~20 minutes
- Spot L4 cost in this region: ~$0.44/hr; estimated total compute spend: **~$0.15**

## Artifacts
Remote output directory:
```
/tmp/fugu-outputs/retrain-fugu-conductor/retrain-conductor-real-3b-smoke-gcp-20260801_152846/
├── checkpoint/
├── dataset.json
├── manifest.json
├── metrics.jsonl
└── pool.json
```

A local copy was pulled to:
```
outputs/conductor_retrain/20260801-real-3b-gcp/retrain-conductor-real-3b-smoke-gcp-20260801_152846/
```

## Key metrics (`metrics.jsonl`)
The one-step training rollout had all-zero reward components for the sampled task; this is expected for a single cold rollout on a new pool. The acceptance test that matters for runtime validity passed:

```
acceptance generation: format_reward=1.0 completion=" 
model_id: [3, 2]
subtasks: ['parse the output file', 'extract and fit G and 2D peaks']
access_list: [[], []]"
```

## Manifest snippet
- `real_checkpoint_loaded`: `true`
- `device`: `cuda`
- `gpu_type`: `NVIDIA L4`
- `gpu_count`: `1`
- `dtype`: `bfloat16`
- `acceptance_passed`: `true`
- `valid_for_runtime`: `true`

See `20260801-real-3b-gcp-manifest.json` for the full manifest.
