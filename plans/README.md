# Implementation Plans

Generated by the improve skill on 2026-08-03 at commit `5874163`. Execute in order. Each executor must read its plan fully, honor STOP conditions, and update its row.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Scope and consume Pi response cache entries | P1 | S | — | DONE |
| 002 | Make Conductor failures recoverable and visible | P1 | M | 001 | DONE |
| 003 | Gate Pi extension behavior in CI | P1 | S | 001–002 | DONE |
| 004 | Stream worker events incrementally and reject corrupt streams | P1 | M | 001–003 | DONE |
| 005 | Calibrate context accounting and remove duplicate history | P2 | M | 004 | DONE |
| 006 | Make coordinator warm-up truthful | P2 | S | 003 | DONE |
| 007 | Bound and cancel backend model work | P2 | M | 004 | DONE |

Status: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED.

Post-plan verification: the Pi final-response bridge now correlates by native `mantis_step` tool-call IDs. Repository mutation between `Verifier ACCEPT` and Pi's follow-up cannot start a second orchestration; replayed tool results fail closed.

## Dependency notes

- 001 stabilizes the provider's two-call Pi bridge before streaming changes.
- 002 defines Conductor event/retry semantics that 003 must lock into CI.
- 004 follows those characterization tests and must preserve the bridge from 001.
- 005 relies on final streaming/usage semantics from 004.
- 007 relies on the transport structure established in 004.

## Verification baseline

```bash
cd extensions && npm run typecheck && bun run tests/stream-test.ts
uv run pytest -q --no-cov tests/test_serve.py
uv run ruff check .
uv run mypy openfugu-patch scripts --exclude outputs
```

At planning time: extension checks passed; `55` focused server tests passed; npm production dependency audit reported no vulnerabilities.

## Findings considered and rejected

- No additional product features are planned. Reliability and native Pi behavior take precedence.
- Training, evaluation, and cloud-launch code were outside this Pi-focused audit.
