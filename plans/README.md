# Implementation Plans

Generated by the improve skill on 2026-08-03 for the llm-router repo (HEAD `aa6d8c4`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Extract and test request-mutation logic | P1 | S | — | DONE |
| 002  | Make config defaults a single source of truth | P1 | S | — | DONE |
| 003  | Eager model load at startup with a truthful ready gate | P2 | M | — | DONE |
| 004  | Make routing latency observable and tunable | P2 | M | — | TODO |
| 005  | Dependency manifest and honest README | P3 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies between plans: 001 touches `server.py`'s handler,
  003/004 touch model loading and `_decide`; 002 touches config defaults and
  005 touches README + a new file. They compose, but run 001 first — it
  establishes the test baseline that makes the other server.py edits safe to
  review (they add no tests themselves).
- 003's startup load and 004's cache both modify `_decide`-adjacent code; if
  both are in flight, apply 003 first (it moves loading out of `_decide`) so
  004's excerpts match.

## Findings considered and rejected

- Static default bearer key + non-constant-time compare (`server.py:35,222-227`):
  not worth doing — localhost-bound personal service with a documented key;
  constant-time compare adds code with no practical threat.
- Fallback-to-expensive on scoring error (`server.py:202-204`): deliberate
  safety default (commit `aa6d8c4` "add fallback handling"); the cost-blowup
  during an OpenAI outage is the accepted trade-off.
- `prompt[:200]` plaintext in `decisions.log` (`server.py:240`): own machine,
  own logs; a privacy filter would add config for a non-problem here.
- Dead `req = httpx.stream(...)` (`server.py:303`): one-line cleanup, folded
  into plan 001 step 3.
- Context-window fallback `1000000` (`server.py:107`): clients over-trust it;
  folded into plan 002 as a logged warning (an honest fallback needs a
  source of truth the model lists can't provide).
- Routing text = last user message only (`server.py:130-139` vs the
  "& context" comment at `server.py:190`): real limitation for agentic
  clients, but changing it shifts routing behavior and needs threshold
  recalibration — flagged, not planned. Revisit after 004's `supra_ms`
  data exists.

## Direction options (not plans — operator's call)

- **D1 — Local routing with the `bert` router**: `ROUTELLM_ROUTER=bert` is
  already supported (`server.py:113-126`); removes the per-request OpenAI
  embeddings dependency (privacy, no `OPENAI_API_KEY` startup hard-fail at
  `llm-router.sh:214-219`, no expensive-default on OpenAI outages). One env
  change + threshold recalibration. Cost: routing quality vs mf.
- **D2 — Analyze `decisions.log`**: every decision is logged
  (`server.py:230-243`) but never consumed; a ~50-line script (cheap/expensive
  split, TTFB, hypothetical savings at other thresholds) makes threshold and
  `SUPRA_MIN_SCORE` tuning (plan 004 step 5) data-driven.
