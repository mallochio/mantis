{
  "name": "mantis-pi-extension",
  "version": "0.2.0",
  "description": "pi extension that routes turns through the local mantis orchestrator",
  "main": "mantis.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc mantis.ts --outDir dist",
    "test": "echo 'no automated tests yet; load mantis.ts into pi and run /mantis auto'"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*",
    "@types/node": "^20",
    "typescript": "^5.5"
  },
  "engines": {
    "node": ">=20"
  }
}
